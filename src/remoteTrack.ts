/**
 * A {@link RemoteTrack} Class handles remote tracks

 */

import { Track } from "./track";

 export class RemoteTrack extends Track{
    /**
      * events getting triggred when first frame comes in
      * @event LocalTrack:first-frame-decoded
      * @instance
      * @memberof remoteTrack
    */
    
    /**
     * returns the userID for the remote track
     * @returns {String} label
     */

        getUserId()  {

        }
    
     }