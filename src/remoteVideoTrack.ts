/**
 * A {@link RemoteVideoTrack} Class handles the basic MediaStreamTrack
 * @extends RemoteTrack
 */

import { RemoteTrack } from "./remoteTrack";

 export class RemoteVideoTrack extends RemoteTrack{

    /**
     * gets the current image data
     * @returns {ImageData}
     */
     getCurrentFrameData(){

    }
 }