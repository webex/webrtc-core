{
  "name": "webrtc-core",
  "version": "1.1.0",
  "scripts": {
    "clean": "rimraf packages/**/.coverage packages/**/dist && rm packets/samples/src/bundle.js",
    "build": "cd packets/webrtc-core && yarn build",
    "start": "cd packets/samples && yarn start"
  },
  "dependencies": {
  }
}
